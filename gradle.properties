# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true

# Fabric Properties
# check these on https://fabricmc.net/develop
# MC 26.x ships with official names baked in — no mappings layer (no Yarn, no
# separate Mojmap remap). The previous build kept `mappings loom.officialMojangMappings()`
# in build.gradle; that line is gone for 26.x.
minecraft_version=26.1.2
loader_version=0.19.2
loom_version=1.16-SNAPSHOT

# Mod Properties
mod_version=0.1.0
maven_group=dev.asante
archives_base_name=matheaufgabenmod

# Dependencies
fabric_version=0.149.0+26.1.2
